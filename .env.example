# Base de datos (Neon Postgres)
#
# `DATABASE_URL`: connection string para runtime (recomendado usar el "pooler" en serverless).
# `DIRECT_URL`: connection string directo (sin pooler) para migrations/introspection de Prisma.
DATABASE_URL="postgresql://USER:PASSWORD@HOST-POOLER:5432/DB?sslmode=require"
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"

# Auth (OBLIGATORIO)
AUTH_SECRET="cambia-este-secreto-largo"

# Vercel Blob (subida de imagenes/documentos)
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# Email (MVP: se recomienda Resend)
FINANCE_EMAIL="finanzas@tudominio.com"
EMAIL_FROM="Godplaces <no-reply@tudominio.com>"
RESEND_API_KEY=""

# OpenAI / ChatKit (Agent Builder)
OPENAI_API_KEY="sk-"
OPENAI_MODEL="gpt-5-mini"
# Provider del chat en el frontend: "local" (endpoint propio) o "chatkit" (Agent Builder)
NEXT_PUBLIC_CHAT_PROVIDER="local"
# Workflow ID de Agent Builder (ej: wf_...)
CHATKIT_WORKFLOW_ID=""
# Opcional: si quieres usar "draft" u otra versión (si se deja vacío usa producción)
CHATKIT_WORKFLOW_VERSION=""

# Seeds (OBLIGATORIO para crear el primer ROOT)
SEED_ROOT_EMAIL="root@godplaces.ve"
SEED_ROOT_PASSWORD="cambia-esta-clave"
SEED_ROOT_NOMBRE="ROOT"

# Seed opcional: usuario de inventario interno
SEED_INTERNAL_EMAIL="inventario@trends172tech.com"
SEED_INTERNAL_PASSWORD=""

# SEO
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
